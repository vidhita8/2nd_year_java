// Matrix Multiplication + Transpose Check
// Write a program to:
// Take two matrices as input,
// Multiply them (if possible),
// Then check if the result matrix is symmetric (i.e., same as its transpose).
import java.util.*;
public class Prob1 {
    public static void main(String[] args) {
        int[] arr={3,5,2,7,3,1};
        Arrays.sort(arr);
        for(int i=0;)
    }
}
